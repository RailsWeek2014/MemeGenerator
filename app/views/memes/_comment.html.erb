<fieldset class="well fieldset-comment">
    <legend class="legend-comment">
        <%= get_username_from_id(comment.user_id) %> - <%= comment.created_at %>
    </legend>
        <p style= "word-wrap: break-word;"><%= comment.comment %></p>
</fieldset>