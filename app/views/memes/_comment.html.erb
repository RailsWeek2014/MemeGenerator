<fieldset class="well fieldset-comment">
    <legend class="legend-comment">
        <% @User = User.find(comment.user_id) %>
        <%= @User.username %> - <%= comment.created_at %>
    </legend>
        <%= comment.comment %>
</fieldset>