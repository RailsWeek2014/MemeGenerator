  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <%= link_to image_tag(get_meme_image_link(meme.id)), meme_path(meme.id.to_s) %>
      <div class="caption">
        <h3><p style= "word-wrap: break-word;"><%= meme.title %></p></h3>
        <p>Erstellt: <%= meme.created_at %></p>
        <p>Wird gelistet:
        <% if meme.isprivate == false %>
        Ja
        <% else %>
        Nein
        <% end %></p>
        <p>
        <p>
        <b>Erstellt von</b>
        <%= get_username_from_id(meme) %>
        </p>
        <%= link_to acp_meme_path(meme.id.to_s), :class => 'btn btn-danger', :method => :delete  do %>
          <i class="glyphicon glyphicon-trash"></i> Löschen
        <% end %>
        </p>
        <p></p>
      </div>
    </div>
  </div>